name: Job Tracker - Fetch Jobs

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: job-tracker-fetch
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r job_tracker/scripts/requirements.txt

      - name: Fetch and aggregate jobs
        run: python job_tracker/scripts/fetch_jobs.py

      - name: Send job alerts
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          ALERT_EMAIL_TO: ${{ secrets.ALERT_EMAIL_TO }}
        run: python job_tracker/scripts/send_alerts.py

      - name: Clean up ephemeral run data
        run: rm -f job_tracker/data/.new_this_run.json

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add job_tracker/data/*.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore(job-tracker): refresh job listings"
            git push
          fi
